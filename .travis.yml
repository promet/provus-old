dist: bionic

language: minimal

install:
  - curl -fsSL https://get.docksal.io | bash
  - fin update
  - fin version
  - fin sysinfo

before_script:
  - fin init

script:
  - fin test

after_success:
  - fin deploy 
